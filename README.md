# TODO... :

1. Create a custom hook for fetching the product list. (20 marks)

2. Use route parameters for product details. (The router should be dynamic)

3. Implement a search feature to filter products (using redux state ) in the ProductList. (20
marks)

3.  Add functionality to allow users to adjust the quantity of products in the cart. However,
the "quantity of each product should not go below 1".

4. Create a new component called OrderSuccess.jsx  -> which displays modal for order success.

5. display a message
saying "Order placed" and empty the cart, then redirect the user back to the Home page
automatically.