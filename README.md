# TODO... :

1. Create a custom hook for fetching the product list. (20 marks)

2. Use route parameters for product details. (The router should be dynamic)

3. Implement a search feature to filter products (using redux state ) in the ProductList. (20
marks)



# Place order workflow:

1. Create a new component called PlaceOrder.jsx  -> which has form for user info. (dynamic routing)
2. Add id to redux slice. (name: OrderedItemsSlice)
3. Create a new component called OrderSuccess.jsx  -> which displays modal for order success.